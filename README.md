# Debark University Technology Club

This is a web-based membership registration system developed for the Debark University Technology Club. It allows students to register their details, which are then stored in a MySQL database.

## 🚀 Features
- Student Registration Form (First Name, Last Name, ID, Dept, Year).
- Database connectivity using PHP and MySQL.
- Responsive Design using CSS.
- Automatic Database and Table creation scripts.

## 🛠️ Installation & Setup
1. Clone this repository to your local machine inside your server directory (e.g., `htdocs` in XAMPP).
2. Start your Apache and MySQL servers.
3. **Database Setup:**
   - Run `createdb.php` first to create the database named `Techonology`.
   - Run `createtable.php` to create the `techono_member` table.
4. Open `home.php` in your browser to access the registration form.

## 💻 Tech Stack
- **Frontend:** HTML, CSS, JavaScript
- **Backend:** PHP
- **Database:** MySQL

## 👤 Author
**Dereje Shiferaw** - 3rd Year IT Student, Debark University.